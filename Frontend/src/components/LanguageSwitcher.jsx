// import React, { useContext } from 'react';
// import { LanguageContext } from '../context/LanguageContext';
// import '../styles/LanguageSwitcher.css';

// const LanguageSwitcher = () => {
//   const { changeLanguage } = useContext(LanguageContext);

//   return (
//     <div className="language-switcher">
//       <button onClick={() => changeLanguage('en')}>English</button>
//       <button onClick={() => changeLanguage('ps')}>Pashto</button>
//       <button onClick={() => changeLanguage('dr')}>Dari</button>
//     </div>
//   );
// };

// export default LanguageSwitcher;

// Explanation:
// useContext(LanguageContext): Accesses the changeLanguage function from the context.

// changeLanguage(lang): Updates the selected language and translations.